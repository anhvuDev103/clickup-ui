import { Flex } from '@uikit';
import styled from 'styled-components';

export const LoginFrame = styled(Flex)`
  .Login_formModal {
    box-shadow: 0 24px 64px #26214a1a;
  }

  .Login_form {
    width: 100%;
  }
`;
