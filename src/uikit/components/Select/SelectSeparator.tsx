import { BoxProps } from '@uikit/types';

import { Divider } from '../Separator';

const SelectSeparator: React.FC<BoxProps> = (props) => {
  return <Divider {...props} />;
};

export default SelectSeparator;
